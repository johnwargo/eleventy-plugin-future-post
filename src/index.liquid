---
layout: default
title: Home
---

{% assign latest_posts = collections.post | reverse %}

Lists all of the posts for the site.

<p>Posts: {{ latest_posts.length}}</p>
<ul>
  {%- for post in latest_posts -%}  
    <li>
      <a href="{{ post.url }}">{{ post.data.title }}</a>
      <p>{{ post.data.date }}</p>
    </li>
  {%- endfor -%}
</ul>
